# AttriSense — pinned dependencies for reproducible installs (Python 3.11+)
# Install: pip install -r requirements.txt && pip install -e .

# Core data & numerics
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0

# Visualization
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.8.0

# Machine learning
scikit-learn==1.9.0
xgboost==3.3.0
shap==0.52.0
joblib==1.5.3

# Application & configuration
streamlit==1.58.0
PyYAML==6.0.3

# Notebooks (optional for dashboard-only use; required for notebook workflow)
jupyter==1.1.1
ipykernel==7.3.0

# Package install (editable mode registers src/attrisense)
# Run after pip install -r requirements.txt:
#   pip install -e .
